[package]
name = "ui"
version = "0.1.0"
edition = "2024"

[dependencies]
crossterm = "0.29.0"
ratatui = "0.30.2"
tokio = { version = "1.52.3", features = ["sync", "rt", "macros"] }
axum = "0.7"
tower-http = { version = "0.5", features = ["fs", "cors"] }
rust-embed = "8.12.0"
mime_guess = "2.0.5"
